{
  "version": "2.0.0",
  "tasks": [
    {
      "type": "docker-run",
      "label": "docker-run: debug",
      "dependsOn": ["docker-build"],
      "options": {
        "cwd": "${workspaceFolder}/backend"
      },
      "python": {
        "module": "uvicorn",
        "args": ["app.main:app", "--reload"]
      }
    }
  ]
}
